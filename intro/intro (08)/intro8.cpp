#include <stdio.h>

int main()
{
    //for(초기식; 조건식; 증감식)
    /*{
        반복실행될 명령문들
    }
    몇 번의 반복이 정해져있을 때 사용하면 좋음
    */

    for (int i = 0; i < 10; i++)
    {
        printf("%d\t", i);
    }

    printf("\n");

    for (int i = 2; i < 10; i++) //단수
  // 괄호를 치지 않아도 밑에 있는 한 문장만 실행할 수있음
    
    {
        for (int j = 2; j < 10; j++) //곱수
            printf("%d X %d = %d\n", i, j, i * j);
        printf("\n");
    }
    printf("==========================\n");

    /*
    2x1=2   3x1=3   4x1=4 . . . 9x1=9
    2x2=4
    .
    .
    .
    2x9=18
    */
    for (int i = 1; i < 10; i++) //단수
        // 괄호를 치지 않아도 밑에 있는 한 문장만 실행할 수있음

    {
        for (int j = 2; j < 10; j++) //곱수
        {
            printf("%d X %d = %d\t", j, i, j * i);
        }
            
        printf("\n");
    }
    printf("==========================\n");

    //12345678910
    for (int i = 1; i <= 100; i++)
    {
        printf("%d\t", i);
        if (i % 10 == 0)
            printf("\n");
    }
    printf("==========================\n");

    printf("*********************369게임*************************************\n");
    for (int i = 1; i <= 100; i++)
    {
        int ten = i / 10; //i에서 10의 자리를 분리
        int il = i % 10; //i에서 1의 자리를 분리
        char ch = 'a';

      
        if (ten % 3 == 0 && ten != 0) //10의 자리수가 3의 배수이고 0이 아니라면
            ch = 'c'; //ch의 값을 'c'로 변경

        if (il % 3 == 0 && ch == 'c' && il != 0) //1의 자리수가 3의 배수이고 10의 자리도 10의 배수라면 
            ch = 'C'; //ch의 값을 'C'로 변경
        else if (il % 3 == 0 && il != 0) //그렇지 않고 1의자리수만 3의 배수라면
            ch = 'C'; //ch에 'c'를 대입

        if (ch == 'c' || ch == 'C') //ch의 값이 'a'가 아니고'c'나'C'아면 숫자 대신 ch를 출력
            printf("%c\t", ch); 
        else
            printf("%d\t", i);//위의 경우에 해당하지 않는다면 i를 그대로 출력

        if (i % 10 == 0)
            printf("\n");
    }
     printf("==========================\n");


     
         for (int i = 1; i <= 100; i++) {
             int ten = i / 10; // 10의 자리를 분리
             int il = i % 10;  // 1의 자리를 분리
             char ch = 'a';    // 기본값은 'a'

             // 10의 자리 수가 3, 6, 9인 경우
             if (ten % 3 == 0 && ten != 0)
                 ch = 'c';  // 'c'로 변경

             // 1의 자리 수가 3, 6, 9이고, 10의 자리 수가 3, 6, 9인 경우
             if (il % 3 == 0 && ch == 'c' && il != 0)
                 ch = 'C';  // 'C'로 변경
             else if (il % 3 == 0 && il != 0)
                 ch = 'C';  // 1의 자리 수만 3, 6, 9일 때 'C'로 변경

             // 'c'는 '짝'으로 출력, 'C'는 '짝짝'으로 출력
             if (ch == 'c')
                 printf("짝\t");
             else if (ch == 'C')
                 printf("짝짝\t");
             else
                 printf("%d\t", i); // 나머지 숫자는 그대로 출력

             // 10의 자리가 0일 때 (즉, 10의 배수마다 한 줄 바꿔서 출력)
             if (i % 10 == 0)
                 printf("\n");
         }

         printf("**********************************************************\n");

    return 0;
}

